name: 01 - Building Blocks

on: # push
  pull_request:
  workflow_dispatch:

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello, World!"
  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: Failed step
        run: |
            # echo "This step will fail"
            # exit 1
            echo "I will succeed"
            exit 0
      - name: Say Goodbye
        run: echo "Goodbye!"
